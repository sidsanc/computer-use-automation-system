# Needed only for discovery runs. Replay never reads it.
ANTHROPIC_API_KEY=

# Fake teller credentials for the local mock app. Referenced by name (secret_ref), never stored in artifacts or logs.
CUA_SECRET_TELLER_USERNAME=teller01
CUA_SECRET_TELLER_PASSWORD=change-me-local-only

# Flask session signing key for the mock app.
MOCKBANK_SECRET_KEY=dev-only-not-secret
